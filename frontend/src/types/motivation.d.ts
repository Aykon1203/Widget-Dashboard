declare module 'motivation' {
  // The package doesn't ship types. Export `any` to simplify usage.
  // You can improve these types later if you want stricter typings.
  const value: any
  export default value
}
